{
  "name": "database",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "db:migrate": "prisma migrate dev",
    "db:generate": "prisma generate",
    "db:studio": "prisma studio"
  },
  "devDependencies": {
    "@prisma/client": "^5.8.0",
    "prisma": "^5.8.0"
  }
}
